fastapi==0.112.0          # The web framework for building the API
uvicorn==0.22.0           # ASGI server to run the FastAPI application
fastapi-mail==1.4.1      # For sending emails with FastAPI
pydantic==2.8.2          # Data validation and settings management
python-dotenv==1.0.0      # For loading environment variables from a .env file
jinja2==3.1.2             # For templating email content (optional, if needed)